-r scripts/requirements.txt   # pull in runtime deps

# Dev/test/tooling
# Pin frictionless; CI workflow depends on stable CLI behaviour
frictionless==5.16.0
pytest>=8.0.0
pre-commit>=3.7.0
ruff>=0.6.9
